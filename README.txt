Installation de NativeScript	1. Install Node.js	2. Avant de lancer la vŽrification de Node.js faire mode Ñversion (vŽrifie si prŽsent)		PrŽsentation de plusieurs Žtapes dÕinstallation via Windows :	1. Install Chocolatey to simplify the installation and configuration of the requirements		Ñ> Run the command prompt as an Administrator		Ñ> Copy and paste the following script in the command prompt.		Ñ> @powershell -NoProfile -ExecutionPolicy unrestricted -Command "iex ((new-object net.webclient).DownloadString('https://chocolatey.org/install.ps1'))" && SET PATH=%PATH%;%ALLUSERSPROFILE%\chocolatey\bin		--> 		Restart the command prompt.	2. Install Google Chrome (required to debug NativeScript apps)		Ñ> In the command prompt, run the following command.		Ñ> choco install googlechrome -y	3. Install the latest Node.js LTS		Ñ> In the command prompt, run the following command.		Ñ> choco install nodejs-lts -y	4. Install JDK 8		Ñ> In the command prompt, run the following command.		Ñ> choco install jdk8 -y		5. Install the Android SDK.		Ñ> In the command prompt, run the following command		Ñ> choco install android-sdk -y		Ñ> Restart the command prompt.	6. Install all packages for the Android SDK Platform 25, Android SDK Build-Tools 25.0.2 or later, Android Support Repository, Google Repository and any other SDKs that you may need. You can alternatively use the following command, which will install all required packages.		Ñ> "%ANDROID_HOME%\tools\bin\sdkmanager" "platform-tools" "platforms;android-25" "build-tools;25.0.2" "extras;android;m2repository" "extras;google;m2repository"	7. Install Android virtual devices (AVDs). There are multiple ways to do it so just choose one:		Ñ> Execute the following command in Command Prompt as Administrator:		Ñ> @powershell -NoProfile -ExecutionPolicy Bypass -Command "iex ((new-object net.webclient).DownloadString('https://nativescript.org/setup/win-avd'))"		Ñ> Use Android Studio and install Android virtual devices (AVDs) from there:		Ñ> Install the Android Studio by running the following command in the command prompt as Administrator:		Ñ> choco install androidstudio -y		Ñ> Restart the command prompt.		Ñ> Open Android Studio > NOTE: If you see a gradle error like "Failed to find Build Tools revision 25.0.2", make sure to click on the install link and fix this error. Otherwise Tools->Android->AVD Manager might not be available.		Ñ> Go to Create and Manage Virtual Devices		Ñ> Follow the steps to create and start AVD with enabled HAXM.	8. Install the NativeScript CLI		Ñ> Run the following command.		Ñ> npm i -g nativescript		Ñ> Restart the command prompt.	9. To check if your system is configured properly, run the following command.		Ñ> tns doctor